---
  - name: "Get Hardware Info"
    hosts: all
    connection: local 
    tasks:

    - name: "Get system hardware information"
      shell: "lshw"
      register: "output"

    - debug: var=output.stdout_lines